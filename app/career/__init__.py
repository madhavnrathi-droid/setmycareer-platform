"""Setmycareer career-intelligence package.

The career lane of the Blueprint pipeline: a faithful encoding of the FOUNDATION
ontology (professional `pc.*` metrics + `cx.*` composites), the deterministic
weighting + confidence engines (LLM proposes, Python decides), and the career
LangGraph agents. Mirrors bloo's mental-health lane; never guarantees an outcome;
signal-language + confidence on every number.
"""
